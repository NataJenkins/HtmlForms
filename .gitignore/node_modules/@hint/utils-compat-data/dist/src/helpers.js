"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const postcss_1 = require("postcss");
exports.getFeatureData = (context, name) => {
    if (!context || context[name]) {
        return [context && context[name], '', name];
    }
    const prefix = postcss_1.vendor.prefix(name);
    const unprefixedName = postcss_1.vendor.unprefixed(name);
    return [context[unprefixedName], prefix, unprefixedName];
};
