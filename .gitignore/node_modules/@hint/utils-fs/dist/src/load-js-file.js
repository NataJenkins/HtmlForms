"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.loadJSFile = (filePath) => {
    let file;
    if (process.env.webpack) {
        file = eval(`require("${filePath}")`);
    }
    else {
        file = require(filePath);
    }
    return file;
};
